# Root-level env vars used by docker-compose.yml at parse time.
# Copy to .env and adjust as needed.
#
# SQL Server password (must be 8+ chars with upper, lower, digit, and symbol).
# This same value is also used as the Postgres password so the full stack only
# needs one DB_PASSWORD.
DB_PASSWORD=SuperSecurePassword123
